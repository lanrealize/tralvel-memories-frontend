<view class="container">
  <block wx:for="{{characters}}" wx:key="index">
    <text 
      class="char {{dynamicWordsAnimationClass}}" 
      style="animation-delay: {{item.delay}}s; color: {{primaryColor}};"
    >{{item.char}}</text>
  </block>
</view>